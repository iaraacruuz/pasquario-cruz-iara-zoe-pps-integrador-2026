import { VibrateOnError } from './vibrate-on-error';

describe('VibrateOnError', () => {
  it('should create an instance', () => {
    const directive = new VibrateOnError();
    expect(directive).toBeTruthy();
  });
});
